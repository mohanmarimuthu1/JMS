# Copy to .env.local (gitignored) and fill in real values.
# Project Settings > API in the Supabase dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Only needed to run scripts/test-rls.mjs and scripts/test-numbering.mjs —
# the single shared login created by hand in Authentication > Users
# (Auto Confirm User). Never the service_role key. See supabase/README.md.
SUPABASE_URL=
SUPABASE_ANON_KEY=
TEST_EMAIL=
TEST_PASSWORD=
